"""
Application package.

Orchestrates use cases by coordinating domain objects.
Defines the **ports** (abstract classes / protocols) that the infrastructure
layer must implement (e.g. repositories, external-service contracts).

Depends on: domain
"""
